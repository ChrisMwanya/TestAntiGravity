@layout.auth({ title: 'Email vérifié - Finance Manager' })

  @slot('main')
    <div class="max-w-md w-full mx-4">
      <div class="bg-white/10 backdrop-blur-md border border-white/20 rounded-2xl p-8 shadow-2xl">
        <div class="text-center mb-8">
          <div class="w-20 h-20 bg-gradient-to-r from-green-500 to-emerald-500 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
          </div>
          <h1 class="text-3xl font-bold bg-gradient-to-r from-green-400 to-emerald-400 bg-clip-text text-transparent mb-2">
            Email vérifié avec succès !
          </h1>
          <p class="text-gray-400">
            Votre compte est maintenant actif
          </p>
        </div>

        @if(flashMessages.has('success'))
          <div class="mb-6 p-4 bg-green-500/20 border border-green-500/50 rounded-xl text-green-300">
            {{ flashMessages.get('success') }}
          </div>
        @endif

        <div class="bg-white/5 border border-white/10 rounded-xl p-6 mb-6">
          <p class="text-gray-300 text-center mb-2">
            🎉 Félicitations ! Votre adresse email a été vérifiée.
          </p>
          <p class="text-gray-400 text-sm text-center">
            Vous allez être redirigé vers votre tableau de bord dans quelques secondes...
          </p>
        </div>

        <a
          href="{{ route('dashboard') }}"
          class="block w-full px-6 py-3 bg-gradient-to-r from-green-500 to-emerald-500 rounded-xl hover:from-green-600 hover:to-emerald-600 transition font-medium text-center"
        >
          Accéder au tableau de bord
        </a>
      </div>
    </div>

    <script>
      // Redirect to dashboard after 3 seconds
      setTimeout(() => {
        window.location.href = '{{ route('dashboard') }}';
      }, 3000);
    </script>
  @endslot
  
@end
