@layout.app({ title: 'Configuration' })

  @slot('main')
    <div class="space-y-8" x-data="{ activeTab: 'accounts' }">
      <div class="flex justify-between items-center">
        <div>
          <h1 class="text-3xl font-bold mb-2">
            @if(isOnboarding)
              Welcome to FinanceApp!
            @else
              Configuration
            @endif
          </h1>
          <p class="text-slate-400">
            @if(isOnboarding)
              Let's set up your accounts and categories before we start. You can always change this later.
            @else
              Manage your accounts, types, and categories.
            @endif
          </p>
        </div>
        @if(isOnboarding)
          <a
            href="/dashboard"
            class="px-6 py-3 bg-gradient-to-r from-purple-500 to-pink-500 rounded-xl hover:from-purple-600 hover:to-pink-600 transition font-medium flex items-center space-x-2"
          >
            <span>Finish Setup</span>
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
              <path
                fill-rule="evenodd"
                d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z"
                clip-rule="evenodd"
              />
            </svg>
          </a>
        @endif
      </div>

      <!-- Tabs Navigation -->
      <div class="flex space-x-1 bg-white/5 p-1 rounded-xl backdrop-blur-md border border-white/10 w-fit">
        <button
          @click="activeTab = 'accounts'"
          :class="{ 'bg-white/10 text-white shadow-lg': activeTab === 'accounts', 'text-slate-400 hover:text-white hover:bg-white/5': activeTab !== 'accounts' }"
          class="px-6 py-2.5 rounded-lg transition font-medium text-sm"
        >
          Accounts
      </button>
        <button
          @click="activeTab = 'types'"
          :class="{ 'bg-white/10 text-white shadow-lg': activeTab === 'types', 'text-slate-400 hover:text-white hover:bg-white/5': activeTab !== 'types' }"
          class="px-6 py-2.5 rounded-lg transition font-medium text-sm"
        >
          Account Types
      </button>
        <button
          @click="activeTab = 'categories'"
          :class="{ 'bg-white/10 text-white shadow-lg': activeTab === 'categories', 'text-slate-400 hover:text-white hover:bg-white/5': activeTab !== 'categories' }"
          class="px-6 py-2.5 rounded-lg transition font-medium text-sm"
        >
          Categories
      </button>
      </div>

      <!-- Accounts Tab -->
      <div x-show="activeTab === 'accounts'" class="space-y-6" style="display: none;">
        <!-- Create Account Form -->
        <div class="bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl p-6">
          <h2 class="text-xl font-bold mb-4">
            Add New Account
          </h2>
          <form
            action="{{ route('accounts.store') }}"
            method="POST"
            class="grid grid-cols-1 md:grid-cols-4 gap-4 items-end"
          >
            {{ csrfField() }}
            <div class="md:col-span-2">
              <label class="block text-sm font-medium mb-2">Account Name</label>
              <input
                type="text"
                name="name"
                required
                class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl focus:ring-2 focus:ring-purple-500 focus:border-transparent outline-none"
                placeholder="e.g., Main Checking"
              />
            </div>
            <div>
              <label class="block text-sm font-medium mb-2">Type</label>
              <select
                name="accountTypeId"
                required
                class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl focus:ring-2 focus:ring-purple-500 focus:border-transparent outline-none [&>option]:bg-slate-900 [&>option]:text-white"
              >
                @each(type in accountTypes)
                  <option value="{{ type.id }}">
                    {{ type.name }}
                  </option>
                @endeach
              </select>
            </div>
            <div>
              <label class="block text-sm font-medium mb-2">Initial Balance</label>
              <input
                type="number"
                name="balance"
                step="0.01"
                required
                class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl focus:ring-2 focus:ring-purple-500 focus:border-transparent outline-none"
                placeholder="0.00"
              />
            </div>
            <button
              type="submit"
              class="md:col-span-4 px-6 py-3 bg-gradient-to-r from-purple-500 to-pink-500 rounded-xl hover:from-purple-600 hover:to-pink-600 transition font-medium w-full md:w-auto md:ml-auto"
            >
              Add Account
          </button>
          </form>
        </div>

        <!-- Accounts List -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          @each(account in accounts)
            <div
              class="bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl p-6 hover:bg-white/10 transition group"
            >
              <div class="flex justify-between items-start mb-4">
                <div>
                  <h3 class="text-xl font-bold">
                    {{ account.name }}
                  </h3>
                  <p class="text-sm text-gray-400">
                    {{ account.accountType.name }}
                  </p>
                </div>
                <form
                  action="{{ route('accounts.destroy', { id: account.id }) }}?_method=DELETE"
                  method="POST"
                  onsubmit="return confirm('Delete this account? All related transactions will be deleted.')"
                >
                  {{ csrfField() }}
                <button
                    type="submit"
                    class="text-red-400 hover:text-red-300 opacity-0 group-hover:opacity-100 transition p-2 hover:bg-red-400/10 rounded-lg"
                  >
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                      <path
                        fill-rule="evenodd"
                        d="M9 2a1 1 0 00-.894.553L7.382 4H4a1 1 0 000 2v10a2 2 0 002 2h8a2 2 0 002-2V6a1 1 0 100-2h-3.382l-.724-1.447A1 1 0 0011 2H9zM7 8a1 1 0 012 0v6a1 1 0 11-2 0V8zm5-1a1 1 0 00-1 1v6a1 1 0 102 0V8a1 1 0 00-1-1z"
                        clip-rule="evenodd"
                      />
                    </svg>
                  </button>
                </form>
              </div>
              <div
                class="text-2xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-white to-slate-400"
              >
                ${{ Number(account.balance).toFixed(2) }}
              </div>
            </div>
          @else
            <div
              class="col-span-full text-center py-12 text-gray-400 bg-white/5 rounded-2xl border border-white/10 border-dashed"
            >
              No accounts yet. Add your first account above!
            </div>
          @endeach
        </div>
      </div>

      <!-- Account Types Tab -->
      <div x-show="activeTab === 'types'" class="space-y-6" style="display: none;">
        <div class="bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl p-6">
          <h2 class="text-xl font-bold mb-4">
            Add New Account Type
          </h2>
          <form action="{{ route('account_types.store') }}" method="POST" class="flex gap-4 items-end">
            {{ csrfField() }}
            <div class="flex-1">
              <label class="block text-sm font-medium mb-2">Type Name</label>
              <input
                type="text"
                name="name"
                required
                class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl focus:ring-2 focus:ring-purple-500 focus:border-transparent outline-none"
                placeholder="e.g., Mobile Money, Crypto"
              />
            </div>
            <button
              type="submit"
              class="px-6 py-3 bg-gradient-to-r from-purple-500 to-pink-500 rounded-xl hover:from-purple-600 hover:to-pink-600 transition font-medium"
            >
              Add Type
          </button>
          </form>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
          @each(type in accountTypes)
            <div
              class="bg-white/5 backdrop-blur-md border border-white/10 rounded-xl p-4 flex justify-between items-center"
            >
              <div class="flex items-center space-x-3">
                <div class="w-2 h-2 rounded-full {{ type.userId ? 'bg-purple-400' : 'bg-slate-400' }}">
                </div>
                <span class="font-medium">{{ type.name }}</span>
                @if(!type.userId)
                  <span class="text-xs bg-white/10 px-2 py-1 rounded-full text-slate-400">System</span>
                @endif
              </div>
              @if(type.userId)
                <form
                  action="{{ route('account_types.destroy', { id: type.id }) }}?_method=DELETE"
                  method="POST"
                  onsubmit="return confirm('Are you sure?')"
                >
                  {{ csrfField() }}
                <button
                    type="submit"
                    class="text-red-400 hover:text-red-300 transition p-2 hover:bg-red-400/10 rounded-lg"
                  >
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                      <path
                        fill-rule="evenodd"
                        d="M9 2a1 1 0 00-.894.553L7.382 4H4a1 1 0 000 2v10a2 2 0 002 2h8a2 2 0 002-2V6a1 1 0 100-2h-3.382l-.724-1.447A1 1 0 0011 2H9zM7 8a1 1 0 012 0v6a1 1 0 11-2 0V8zm5-1a1 1 0 00-1 1v6a1 1 0 102 0V8a1 1 0 00-1-1z"
                        clip-rule="evenodd"
                      />
                    </svg>
                  </button>
                </form>
              @endif
            </div>
          @endeach
        </div>
      </div>

      <!-- Categories Tab -->
      <div x-show="activeTab === 'categories'" class="space-y-6" style="display: none;">
        <div class="bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl p-6">
          <h2 class="text-xl font-bold mb-4">
            Add New Category
          </h2>
          <form action="{{ route('categories.store') }}" method="POST" class="flex gap-4 items-end">
            {{ csrfField() }}
            <div class="flex-1">
              <label class="block text-sm font-medium mb-2">Name</label>
              <input
                type="text"
                name="name"
                required
                class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl focus:ring-2 focus:ring-purple-500 focus:border-transparent outline-none"
                placeholder="e.g., Freelance, Hobbies"
              />
            </div>
            <div class="w-48">
              <label class="block text-sm font-medium mb-2">Type</label>
              <select
                name="type"
                required
                class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl focus:ring-2 focus:ring-purple-500 focus:border-transparent outline-none [&>option]:bg-slate-900 [&>option]:text-white"
              >
                <option value="expense">
                  Expense
                </option>
                <option value="income">
                  Income
                </option>
              </select>
            </div>
            <button
              type="submit"
              class="px-6 py-3 bg-gradient-to-r from-purple-500 to-pink-500 rounded-xl hover:from-purple-600 hover:to-pink-600 transition font-medium"
            >
              Add Category
          </button>
          </form>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          <!-- Income Categories -->
          <div class="space-y-4">
            <h3 class="text-xl font-bold text-green-400">
              Income Categories
            </h3>
            <div class="bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl overflow-hidden">
              @each(category in categories.filter(c => c.type === 'income'))
                <div
                  class="p-4 border-b border-white/10 last:border-0 flex justify-between items-center hover:bg-white/5 transition"
                >
                  <div class="flex items-center space-x-3">
                    <div class="w-2 h-2 rounded-full {{ category.userId ? 'bg-purple-400' : 'bg-slate-400' }}">
                    </div>
                    <span class="font-medium">{{ category.name }}</span>
                    @if(!category.userId)
                      <span class="text-xs bg-white/10 px-2 py-1 rounded-full text-slate-400">System</span>
                    @endif
                  </div>
                  @if(category.userId)
                    <form
                      action="{{ route('categories.destroy', { id: category.id }) }}?_method=DELETE"
                      method="POST"
                      onsubmit="return confirm('Are you sure?')"
                    >
                      {{ csrfField() }}
                    <button
                        type="submit"
                        class="text-red-400 hover:text-red-300 transition p-2 hover:bg-red-400/10 rounded-lg"
                      >
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                          <path
                            fill-rule="evenodd"
                            d="M9 2a1 1 0 00-.894.553L7.382 4H4a1 1 0 000 2v10a2 2 0 002 2h8a2 2 0 002-2V6a1 1 0 100-2h-3.382l-.724-1.447A1 1 0 0011 2H9zM7 8a1 1 0 012 0v6a1 1 0 11-2 0V8zm5-1a1 1 0 00-1 1v6a1 1 0 102 0V8a1 1 0 00-1-1z"
                            clip-rule="evenodd"
                          />
                        </svg>
                      </button>
                    </form>
                  @endif
                </div>
              @endeach
            </div>
          </div>

          <!-- Expense Categories -->
          <div class="space-y-4">
            <h3 class="text-xl font-bold text-red-400">
              Expense Categories
            </h3>
            <div class="bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl overflow-hidden">
              @each(category in categories.filter(c => c.type === 'expense'))
                <div
                  class="p-4 border-b border-white/10 last:border-0 flex justify-between items-center hover:bg-white/5 transition"
                >
                  <div class="flex items-center space-x-3">
                    <div class="w-2 h-2 rounded-full {{ category.userId ? 'bg-purple-400' : 'bg-slate-400' }}">
                    </div>
                    <span class="font-medium">{{ category.name }}</span>
                    @if(!category.userId)
                      <span class="text-xs bg-white/10 px-2 py-1 rounded-full text-slate-400">System</span>
                    @endif
                  </div>
                  @if(category.userId)
                    <form
                      action="{{ route('categories.destroy', { id: category.id }) }}?_method=DELETE"
                      method="POST"
                      onsubmit="return confirm('Are you sure?')"
                    >
                      {{ csrfField() }}
                    <button
                        type="submit"
                        class="text-red-400 hover:text-red-300 transition p-2 hover:bg-red-400/10 rounded-lg"
                      >
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                          <path
                            fill-rule="evenodd"
                            d="M9 2a1 1 0 00-.894.553L7.382 4H4a1 1 0 000 2v10a2 2 0 002 2h8a2 2 0 002-2V6a1 1 0 100-2h-3.382l-.724-1.447A1 1 0 0011 2H9zM7 8a1 1 0 012 0v6a1 1 0 11-2 0V8zm5-1a1 1 0 00-1 1v6a1 1 0 102 0V8a1 1 0 00-1-1z"
                            clip-rule="evenodd"
                          />
                        </svg>
                      </button>
                    </form>
                  @endif
                </div>
              @endeach
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Alpine.js for Tabs -->
    <script src="//unpkg.com/alpinejs" defer>

    </script>
  @endslot
@end
